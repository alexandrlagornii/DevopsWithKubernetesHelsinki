apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: randomimage-claim
  namespace: project
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
