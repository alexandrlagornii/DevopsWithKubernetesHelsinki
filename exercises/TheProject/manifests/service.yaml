apiVersion: v1
kind: Service
metadata:
  name: theproject-svc
  namespace: project
spec:
  type: NodePort
  selector:
    app: theproject
  ports:
    - port: 2347
      protocol: TCP
      targetPort: 80 
