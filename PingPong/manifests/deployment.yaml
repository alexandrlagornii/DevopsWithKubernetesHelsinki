apiVersion: apps/v1
kind: Deployment
metadata:
  name: pingpong
  namespace: exercises
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pingpong
  template:
    metadata:
      labels:
        app: pingpong
    spec:
      containers:
        - name: logoutput
          image: alago0/pingpong:8-30-2025
          volumeMounts:
          - name: pingpong-configmap-volume
            mountPath: /out/appsettings.json
            subPath: appsettings.json
      volumes:
        - name: pingpong-configmap-volume
          configMap:
            name: pingpong-configmap
