apiVersion: v1
kind: ConfigMap
metadata:
  name: todobackend-configmap
  namespace: project
data:
  appsettings.json: |
    {
      "Logging": {
        "LogLevel": {
          "Default": "Information",
          "Microsoft.AspNetCore": "Warning"
        }
      },
      "AllowedHosts": "*"
    }